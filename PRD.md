# 今日热搜 · 产品需求文档（PRD）

## 产品概述

「今日热搜」是一个 **AI 增强的多平台热搜聚合网站**。聚合微博、知乎、B站、澎湃新闻、36氪、虎扑、GitHub 共 7 个核心平台的实时热榜（抖音尽力接入，可降级），并通过 AI 实现跨平台事件聚类、智能推荐和分类标签，帮助用户 3 分钟内掌握全网正在发生什么。

**一句话定位**：用 AI 读懂热搜 —— 不只是聚合，而是理解。

**MVP 定义**：今日热搜首版是一个无需登录、可公网访问的 AI 增强热榜聚合网站。它稳定展示 7 个核心平台的实时热榜，并尽力接入抖音；通过 AI 生成今日推荐和分类标签，帮助用户在 3 分钟内理解全网核心热点。AI 或单个平台不可用时，基础热榜浏览不受影响。

## 目标用户

### 产品目标用户

- 想快速了解全网热点的上班族、学生、信息工作者
- 核心使用场景：晨间通勤 3 分钟速览、午休碎片时间刷热点
- 关注点：信息效率，一处看完全平台热点

### 项目展示受众

- 对 Vibe Coding / 全栈开发感兴趣的学习者
- 关注点：开发过程、技术实现、AI 集成方式
- 本类受众不影响产品功能设计决策

## 核心功能

### MVP 功能清单

#### F1：AI 推荐卡片（首页置顶）

- 位于首页最顶部，视觉上与普通平台卡片区分（更大、更醒目）
- AI 从全平台热搜中筛选出 **5~8 个最值得关注的核心事件**
- 每个事件展示：
  - 事件标题（AI 生成的归纳性标题）
  - 推荐等级：S（必看）/ A（推荐）/ B（了解）
  - 推荐理由：一句话，必须引用可解释依据（如「多平台同时登榜」「排名靠前」「热度飙升」）
  - 涉及平台列表（如「微博 #1 · 知乎 #3」）
  - 情感倾向标记：正面 / 中性 / 负面（无法判断时默认中性）
- 降级策略：AI 挂掉时该卡片隐藏或显示「AI 推荐暂时不可用」，不影响其他平台卡片

**AI 输入字段**：

| 字段 | 来源 | 说明 |
|------|------|------|
| platform | 平台标识 | weibo / zhihu / bilibili 等 |
| rank | 热搜排名 | 数字 |
| title | 热搜标题 | 原始标题文本 |
| heat | 热度值 | 字符串，可能为空 |
| url | 原始链接 | 完整 URL |
| fetchedAt | 抓取时间 | ISO8601 |

**AI 输出字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| eventTitle | string | AI 归纳的事件标题 |
| level | S / A / B | 推荐等级 |
| reason | string | 推荐理由（一句话，引用可解释依据） |
| platforms | array | 涉及平台及排名，如 `[{"source": "weibo", "rank": 1}]` |
| sentiment | 正面 / 中性 / 负面 | 情感倾向 |
| relatedItems | array | 关联的热搜条目索引 |

**验收标准**：

- 输出 5~8 条事件，同一明显事件不重复出现
- 推荐理由必须引用可解释依据，不能是空话
- 情感倾向只允许三值枚举，无法判断时默认中性
- AI 返回格式异常时后端兜底，不影响基础热搜展示
- AI 响应超时（>10s）时跳过，返回空推荐

#### F2：平台热榜卡片网格

- 7 个核心平台（微博、知乎、B站、澎湃、36氪、虎扑、GitHub）各一张卡片
- 抖音为可降级平台：成功接入则展示，失败则隐藏或显示「数据源维护中」
- 每张卡片包含：
  - 平台图标 + 平台名称 + 榜单名称（如「微博 · 热搜榜」）
  - Top 15~20 条热搜列表
  - 每条：排名、标题（可点击跳转原链接，新标签页打开）、热度值（有则显示，缺失则隐藏）
  - **AI 分类标签**：每条热搜旁显示 1~2 个彩色小标签（如「科技」「娱乐」）
  - 底部显示「更新于 x 分钟前」
- 排名 1~3 视觉强调（不同颜色/样式）
- 单平台失败时该卡片显示错误态 + 重试按钮，不影响其他卡片
- 原链接缺失时标题不可点击，显示为纯文本

#### F3：AI 分类标签筛选

- 固定标签枚举：`科技` `娱乐` `体育` `财经` `社会` `游戏` `教育` `国际` `生活` `其他`
- 每条热搜最多 2 个标签
- AI 无法分类时使用「其他」
- AI 返回非法标签（不在枚举内）时后端自动过滤
- 前端只展示固定枚举内的标签
- 首页顶部（AI 推荐卡片下方）提供标签筛选栏
- 用户点击某个标签后，所有平台卡片仅显示匹配该标签的热搜条目
- 点击「全部」恢复完整列表
- **标签筛选无结果时**：显示「该分类下暂无热搜」空状态提示

#### F4：数据接口（自建后端）

- `GET /api/hot` — 返回全部平台数据 + AI 推荐卡片 + 分类标签
- `GET /api/hot/:source` — 返回单平台数据
- source 枚举：weibo | zhihu | bilibili | douyin | thepaper | 36kr | hupu | github
- 无效 source 返回 404

**基础数据契约**（成功响应）：

```json
{
  "updatedAt": "2026-06-03T10:00:00Z",
  "ai": {
    "available": true,
    "recommendations": [
      {
        "eventTitle": "某事件标题",
        "level": "S",
        "reason": "多平台同时登榜，微博知乎B站均进前5",
        "platforms": [{"source": "weibo", "rank": 1}, {"source": "zhihu", "rank": 3}],
        "sentiment": "中性",
        "relatedItems": [0, 15, 32]
      }
    ]
  },
  "sources": [
    {
      "source": "weibo",
      "name": "微博",
      "listName": "热搜榜",
      "status": "ok",
      "updatedAt": "2026-06-03T10:00:00Z",
      "items": [
        {
          "rank": 1,
          "title": "示例热搜标题",
          "url": "https://example.com",
          "heat": "123万",
          "tags": ["社会"]
        }
      ]
    }
  ]
}
```

**错误态数据结构**：

```json
{
  "source": "douyin",
  "name": "抖音",
  "listName": "热点榜",
  "status": "error",
  "errorMessage": "数据源暂时不可用",
  "updatedAt": null,
  "items": []
}
```

**AI 不可用时**：

```json
{
  "ai": {
    "available": false,
    "recommendations": []
  }
}
```

#### F5：缓存机制

- 每个平台独立缓存，TTL 5~10 分钟
- AI 推荐和分类标签与热搜数据同步缓存
- 页面展示「更新于 x 分钟前」（基于 updatedAt 计算相对时间）
- 开发环境支持 `?refresh=1` 强制跳过缓存
- 缓存过期但新一轮抓取失败时：返回过期缓存数据并标注「数据可能已过期」

#### F6：异常处理与降级

| 异常场景 | 处理方式 |
|---------|---------|
| 单平台抓取失败 | 该卡片显示错误提示 + 重试按钮，其他卡片正常 |
| 抖音接入失败 | 隐藏卡片或显示「数据源维护中」 |
| AI 服务不可用 | AI 推荐卡片隐藏或显示占位提示，标签显示为空，热搜正常 |
| AI 返回 JSON 格式异常 | 后端兜底，丢弃异常数据，AI 区域降级 |
| 后端整体不可用 | 前端显示全局错误页 |
| 首次加载中 | 骨架屏或 loading 动画 |
| 标签筛选无结果 | 显示「该分类下暂无热搜」 |
| 平台返回空列表 | 该卡片显示「暂无数据」 |
| 部分热搜没有标签 | 正常显示热搜条目，标签位置留空 |
| 用户点击刷新后仍失败 | 保留旧数据，顶部显示轻量提示「刷新失败，显示的是之前的数据」 |

#### F7：刷新按钮

- 顶栏右侧放置刷新按钮
- 普通点击：重新请求 `/api/hot`，走后端缓存（不强制绕过）
- 刷新中显示 loading 状态（按钮旋转或禁用）
- 刷新成功：更新数据和「更新于」时间
- 刷新失败：保留旧数据，显示轻量 toast 提示

#### F8：响应式布局

- 桌面端（>1200px）：AI 推荐卡片占满宽度，平台卡片 3~4 列网格
- 平板端（768~1200px）：平台卡片 2 列
- 手机端（<768px）：平台卡片 1 列，AI 推荐卡片精简展示（仍在首屏可读）

#### F9：页脚与合规

- 站点名称 + 简介
- 声明：本站为个人学习项目，数据来源于各平台公开信息，非官方
- 所有标题点击跳转到来源平台原文
- 更新频率说明（约 x 分钟）
- 侵权联系方式（邮箱占位）
- AI 内容标注：「AI 推荐和标签由 AI 自动生成，仅供参考，本站不对 AI 生成内容作事实保证」

### 本期不做

| 功能 | 不做原因 |
|------|---------|
| 小红书平台 | 无公开 API，反爬极高，MVP 阶段风险不可控 |
| 用户登录/注册 | 纯浏览型工具，登录增加复杂度且阻碍即开即用体验 |
| 历史热度回溯 | 需持久化数据库，存储成本高，MVP 只展示实时数据 |
| 站内搜索 | 需全文索引，工程量大，标签筛选可满足基本过滤需求 |
| 个性化排序 | 依赖登录态，与"不做登录"联动 |

## 界面设计要求

### 整体风格

- 简洁现代，信息密度高，参考今日热榜的清爽风格
- 浅色背景，卡片白底带轻微阴影
- 整页 max-width 限制（约 1400px），居中显示

### 页面结构（自上而下）

```
┌──────────────────────────────────────────────┐
│  顶栏：站名「今日热搜」+ 一句话介绍 + 刷新按钮  │
├──────────────────────────────────────────────┤
│  AI 推荐卡片（占满宽度，特殊视觉样式）          │
│  ┌─ S级事件 ──────────────────────────────┐  │
│  │  A级事件                                │  │
│  │  B级事件                                │  │
│  └────────────────────────────────────────┘  │
├──────────────────────────────────────────────┤
│  标签筛选栏：[全部] [科技] [娱乐] [财经] ...    │
├──────────────────────────────────────────────┤
│  平台卡片网格（3~4 列）                        │
│  ┌─微博──┐ ┌─知乎──┐ ┌─B站───┐ ┌─抖音*─┐  │
│  │ ...   │ │ ...   │ │ ...   │ │ ...   │  │
│  └───────┘ └───────┘ └───────┘ └───────┘  │
│  ┌─澎湃──┐ ┌─36氪──┐ ┌─虎扑──┐ ┌─GitHub─┐  │
│  │ ...   │ │ ...   │ │ ...   │ │ ...   │  │
│  └───────┘ └───────┘ └───────┘ └───────┘  │
│                          * 抖音为可降级平台    │
├──────────────────────────────────────────────┤
│  页脚：合规声明 + AI 内容标注                   │
└──────────────────────────────────────────────┘
```

### 卡片内细节

- 排名 1~3：金/银/铜色圆形数字徽章
- 排名 4+：灰色普通数字
- AI 标签：小圆角彩色药丸（不同类别不同颜色），来自固定枚举
- 热度值：灰色小字，右对齐，缺失时隐藏
- 标题：默认色，hover 变色，点击新标签页打开原链接

## 技术栈

- 前端：React + TypeScript + Vite + CSS Modules
- 后端：Node.js + Express + TypeScript
- AI：DeepSeek V3.2 API
- 部署：不锁死方案，Vercel + Railway 为参考示例，可根据技术设计调整

## 成功指标

### 工程验收指标

- 7 个核心平台卡片全部展示真实数据
- 抖音有可解释的降级方案（成功接入或明确的降级态）
- AI 推荐卡片正确输出 5~8 条推荐事件
- AI 分类标签覆盖率 > 90%
- 单平台故障时其他平台不受影响
- AI 不可用时基础热搜正常展示
- 公网 HTTPS 可访问

### 产品体验指标

- 首页首屏加载时间 < 2 秒
- 数据更新延迟不超过 10 分钟
- 用户无需登录即可完成全部浏览操作
- AI 推荐卡片在移动端首屏可读
- 标签筛选无结果时有明确空状态
